import numpy as np

class Kinegram(object):
    """A class for generating Kinegrams"""
    def __init__(self, arg):
        self.arg = arg

    # def generateKinegramBack(images, animationWidth):
        # interlace images
        # interlace(images, animationWidth)
        # 

    # def interlace(images, width, orientation='vertical'):
        # outputImage = np.copy(images[0].shape())
        # if orientation is 'vertical':
        #    
        #
        # outputImage =
        # for image in images: